# Contributing

Run `go fmt ./...`, `go test ./...`, `go vet ./...`, and `go build ./cmd/labdns` before opening a pull request. Provider changes require mock integration coverage and must preserve unmanaged records.
